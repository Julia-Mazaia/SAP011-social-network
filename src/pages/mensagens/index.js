export default () => {
    const container = document.createElement ('div');

    const template = ` `;
    // adicionar template aqui
    
    container.innerHTML = template;

    return container;
}